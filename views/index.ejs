<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To-Do List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css"> <!-- Optional if you want to add custom styles -->
</head>
<body>
    <div class="container">
        <h1 class="text-center">To-Do List</h1>

        <!-- Input for new task and category -->
        <div class="input-group mb-3">
            <input type="text" class="form-control" id="taskInput" placeholder="Add a task">
            <select id="categoryInput" class="form-control">
                <option value="Work">Work</option>
                <option value="Personal">Personal</option>
                <option value="Urgent">Urgent</option>
            </select>
            <div class="input-group-append">
                <button class="btn btn-primary" onclick="addTask()">Add</button>
            </div>
        </div>

        <!-- Search input field -->
        <input type="text" id="searchInput" class="form-control mb-3" oninput="searchTasks()" placeholder="Search tasks...">

        <!-- Task list -->
        <ul class="list-group" id="taskList"></ul>
    </div>

    <!-- Client-side JavaScript -->
    <script src="/client.js"></script>
</body>
</html>
